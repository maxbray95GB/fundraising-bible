# bible

Password-protected static page served at bible.kindredcapital.vc via GitHub Pages.

The page content is AES-256 encrypted with [StatiCrypt](https://github.com/robinmoisson/staticrypt) — this repo contains only ciphertext. To update the page, re-export the Google Doc as HTML, re-encrypt with StatiCrypt, and replace index.html.
